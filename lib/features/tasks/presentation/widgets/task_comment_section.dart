import 'package:flutter/material.dart';

class TaskCommentSection extends StatelessWidget {
  const TaskCommentSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TaskCommentSection'));
  }
}
